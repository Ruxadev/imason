<h1>Worker Details</h1>

<%= render @worker %>

<%= link_to "Edit this Worker", edit_project_worker_path(@worker.project, @worker), class: "btn btn-primary" %> |
<%= button_to "Destroy this Worker", project_worker_path(@worker.project, @worker), method: :delete, class: "btn btn-danger" %> |
<%= link_to "Back to Workers", project_workers_path(@worker.project), class: "btn btn-secondary" %>
