<% content_for :title, "New worker" %>

<h1>New Worker</h1>

<div class="container">
  <%= render "form", worker: @worker %> <!-- Renders the form partial for new worker -->
</div>

<div class="mt-3">
  <%= link_to "Back to workers", workers_path, class: "btn btn-secondary" %>
</div>
